# Create two variables: top_speed and distance
top_speed=160
distance=300

# Display the values referenced by the variables
print('Top speed is '+ str(top_speed))
print('The distance traveled is ' + str(distance))